% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/serialize.R
\name{serialize_url}
\alias{serialize_url}
\title{Serialize URLs to a standard's own full-string form}
\usage{
serialize_url(
  url,
  standard = c("whatwg", "rfc3986"),
  form = c("source", "normalized"),
  engine = NULL
)
}
\arguments{
\item{url}{A character vector of URLs.}

\item{standard}{The standard to serialize to: \code{"whatwg"} (default) or
\code{"rfc3986"}. Unlike the parse surface, \code{NULL} is not accepted: the parse
surface's \code{url_standard = NULL} is a frozen legacy profile that names no
standard (ADR 0007), so there is nothing to serialize \emph{as}. Passing
\code{NULL} is an error rather than a silent \code{"whatwg"}.}

\item{form}{For \code{standard = "rfc3986"} only, the RFC posture: \code{"source"}
(default, source-preserving) or \code{"normalized"}. Ignored for \code{"whatwg"},
whose serializer has a single spec-defined form.}

\item{engine}{Optional \code{psl_engine} object from \code{pslr::psl_engine()} for
per-request Public Suffix List resolution. \code{NULL} (default) uses the
session-global engine.}
}
\value{
A character vector the same length as \code{url}. \code{NA_character_} for
input the selected standard's parser does not accept.
}
\description{
Renders each URL as the selected standard would serialize it: the
\strong{full string}, credentials and fragment included, with a present-but-empty
\verb{?} or \verb{#} delimiter preserved. This is rurl's \emph{standard serialization}
surface, and it is deliberately \strong{not} \code{\link[=get_clean_url]{get_clean_url()}}.
}
\section{Which surface you want}{


\code{serialize_url()} answers \emph{"what does this URL look like under the URL
Standard / RFC 3986?"}. \code{\link[=get_clean_url]{get_clean_url()}} answers \emph{"what is the canonical,
tidied form of this URL for SEO or deduplication?"}. They are different
products, not two settings of one:
\itemize{
\item \code{serialize_url()} preserves userinfo, the fragment, and empty delimiters;
takes \strong{no} presentation options; and is the substrate rurl's conformance
claims are measured on.
\item \code{\link[=get_clean_url]{get_clean_url()}} drops userinfo and the fragment by design, and is driven
by cleaning policy (\code{www_handling}, \code{trailing_slash_handling},
\code{index_page_handling}, query filtering, port handling, and so on).
}

Because a standard serialization is an \emph{identity}, \code{serialize_url()} accepts
no presentation arguments at all. There is no \code{port_handling},
\code{trailing_slash_handling} or \code{path_encoding} to pass; asking a serializer to
strip a trailing slash would be a category error.
}

\section{Parse posture}{


Each standard is parsed under its own spec posture -- the \code{"whatwg"} and
\code{"rfc-syntax"} profiles (see \code{\link[=url_profile]{url_profile()}}). Both accept any scheme, and
both \strong{require} a scheme: neither standard defines a base-URL-free parse of
\code{example.com/x}, so scheme-less input returns \code{NA} rather than being silently
upgraded to \verb{https://}. Input that the standard's parser rejects also returns
\code{NA}.
}

\section{Standards and forms}{


\describe{
\item{\code{standard = "whatwg"}}{The WHATWG URL Standard's URL serializer
(\verb{#concept-url-serializer}). Spec-exact, which makes credentials lossy in
one direction: WHATWG appends credentials only when the username or
password is non-empty, so \verb{http://@h/} serializes as \verb{http://h/} and
\verb{http://u:@h/} as \verb{http://u@h/}. Both are pinned by the Web Platform
Tests. \code{form} is ignored.}
\item{\verb{standard = "rfc3986", form = "source"}}{RFC 3986 section 5.3
component recomposition with \strong{no} normalization: source bytes are
preserved and the undivided \code{userinfo} slice is emitted verbatim (RFC
3986 has no username/password split), so every credential spelling \verb{u@},
\verb{u:@}, \verb{:p@}, \code{@} -- survives.}
\item{\verb{standard = "rfc3986", form = "normalized"}}{Adds RFC 3986 section
6.2.2 syntax-based normalization (scheme and host case, percent-encoding
triplet case and unreserved-octet decoding, dot-segment removal) and the
section 6.2.3 default-port elision.}
}

Both RFC forms are exposed because choosing one would forfeit either the
round-trip oracle (\code{source}) or the normalized comparison substrate
(\code{normalized}).
}

\examples{
# The fragment and credentials survive; clean_url drops both by design.
serialize_url("http://user:pw@Example.COM:80/a/../b?q=1#frag")
get_clean_url("http://user:pw@Example.COM:80/a/../b?q=1#frag")

# A present-but-empty delimiter carries information and is preserved.
serialize_url(c("http://example.com/", "http://example.com/#",
                "http://example.com/?"))

# RFC 3986: source-preserving versus normalized.
serialize_url("HTTP://Example.COM:80/a/\%7Euser/../x", standard = "rfc3986")
serialize_url("HTTP://Example.COM:80/a/\%7Euser/../x", standard = "rfc3986",
              form = "normalized")

# Any scheme is accepted; no scheme is not.
serialize_url(c("urn:ietf:rfc:2648", "mailto:a@b.com", "foo://h/x"))
serialize_url("example.com/x")

}
\seealso{
\code{\link[=get_clean_url]{get_clean_url()}} for the cleaning surface, \code{\link[=safe_parse_url]{safe_parse_url()}} for
the parsed components, and \code{\link[=url_profile]{url_profile()}} for the parse postures used
here.
}

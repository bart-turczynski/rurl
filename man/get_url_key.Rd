% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/url-key.R
\name{get_url_key}
\alias{get_url_key}
\title{URL comparison key}
\usage{
get_url_key(url, policy = url_key_policy())
}
\arguments{
\item{url}{A character vector of URLs. Factors are coerced.}

\item{policy}{A \code{rurl_url_key_policy} object from \code{\link[=url_key_policy]{url_key_policy()}},
which is also the default. The policy is scalar and is never recycled.}
}
\value{
A classed character vector (\code{rurl_url_key}) the same length as
\code{url}, preserving its names. \code{NA} for a non-keyable element. The policy
version, schema version, standard, scheme-equality mode and the per-element
\code{keyability} reasons ride along as attributes.
}
\description{
Projects each URL onto a versioned, non-URL comparison key: the value rurl
uses to decide whether two URLs identify the same web resource. Use it to
deduplicate, group, or match URLs without relying on a cleaned display
string.
}
\section{The key is not a URL}{


The returned object is a classed character vector whose contents are
injectively framed component bytes, not a URL. Never parse it, never render
it to users, and never reconstruct a URL from it. \code{print()} deliberately
shows a truncated diagnostic form for that reason. What it \emph{is} good for is
comparison: \code{==}, \code{\link[=match]{match()}}, \code{\link[=duplicated]{duplicated()}}, \code{\%in\%} and the \link{url_join}
family all work on it directly.

Framing is length-prefixed, so component boundaries cannot be forged. A host
or path containing separators, control bytes or colons can never make two
different URLs collide.
}

\section{Non-keyable input}{


A URL the selected standard cannot parse has no identity, so its key is
\code{NA} and never matches anything -- not even another \code{NA}. The reason is kept
alongside rather than collapsed into the \code{NA}, and is readable with
\code{attr(key, "keyability")}: one of \code{"ok"}, \code{"missing-input"}, \code{"empty-input"}
or \code{"invalid-parse"}. Missing input is never conflated with an invalid
parse.
}

\examples{
# Presentation differences that are not identity differences.
get_url_key(c("http://example.com:80/a", "http://example.com/a"))

# The fragment and userinfo are excluded from web-resource identity.
k <- get_url_key(c("http://u:pw@example.com/a#top", "http://example.com/a"))
k[1] == k[2]

# Query order and duplicates are significant.
k <- get_url_key(c("http://example.com/?a=1&b=2",
                   "http://example.com/?b=2&a=1"))
k[1] == k[2]

# Deduplicate by identity rather than by string.
u <- c("HTTP://Example.com/a", "http://example.com/a", "http://example.com/b")
u[!duplicated(get_url_key(u))]

# Non-keyable input carries a typed reason.
k <- get_url_key(c("http://example.com/", NA, "", ":::"))
attr(k, "keyability")

}
\seealso{
\code{\link[=url_key_policy]{url_key_policy()}} for the dials, \link{url_join} for joining on the
key, and \code{\link[=serialize_url]{serialize_url()}} for a standard's full-string serialization
(which \emph{is} a URL, unlike this).
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/accessors.R
\name{query_param_summary}
\alias{query_param_summary}
\title{Summarize query parameters across a set of URLs}
\usage{
query_param_summary(
  urls,
  level = c("param", "value"),
  params_keep = NULL,
  params_drop = NULL,
  params_case_sensitive = FALSE,
  empty_param_handling = c("keep", "drop"),
  decode_plus = FALSE
)
}
\arguments{
\item{urls}{A character vector of URLs.}

\item{level}{One of "param" (default) for one row per distinct parameter
name, or "value" for one row per distinct (parameter, value) pair.}

\item{params_keep}{Character vector of parameter-name globs (only \code{*} is
special), or \code{NULL} (default). In "filter" mode this is the rescue list; in
"allow" mode it is the allowlist. Ignored in "drop"/"keep".}

\item{params_drop}{Character vector of parameter-name globs to add to the
built-in denylist in "filter" mode, or \code{NULL} (default). Ignored in
"drop"/"allow"/"keep".}

\item{params_case_sensitive}{Logical (default \code{FALSE}). Controls whether the
denylist and \code{params_keep}/\code{params_drop} matching is case-sensitive.}

\item{empty_param_handling}{One of "keep" (default) or "drop". "drop" removes
empty-valued params (e.g. \verb{?ref=}), except those rescued by \code{params_keep}
in "filter" mode.}

\item{decode_plus}{Logical (default \code{FALSE}). When \code{TRUE}, \code{+} in query
values is treated as a space (HTML-form decoding) before percent-decoding.
\code{FALSE} keeps \code{+} literal (RFC 3986 generic behavior).}
}
\value{
A flat (long) \code{data.frame}. For \code{level = "param"}: \code{param}, \code{n}
(total occurrences), \code{n_urls} (distinct URLs containing the param),
\code{example_value}, \code{example_url}, \code{would_drop}. For \code{level = "value"}:
\code{param}, \code{value}, \code{n}, \code{n_urls}, \code{example_url}, \code{would_drop}. The
\verb{example_*} columns and the param-level \code{would_drop} reflect the
first-seen occurrence (deterministic given input order). Returns a
zero-row \code{data.frame} with the level's columns when no URL carries a query.
}
\description{
Tabulates which query parameters appear across a vector of URLs and what
values they take, with a \code{would_drop} column previewing what
\code{query_handling = "filter"} would remove. Useful for auditing a URL set
before choosing a cleaning policy: see the trackers before you strip them.
}
\details{
Parameter names are grouped \emph{faithfully} (case-sensitively and by their
decoded spelling), so \code{utm_source} and \code{UTM_SOURCE} are reported as separate
rows. The \code{would_drop} preview, by contrast, honours \code{params_case_sensitive}:
with the default \code{params_case_sensitive = FALSE}, \code{UTM_SOURCE} matches the
built-in denylist and shows \code{would_drop = TRUE}; set it to \code{TRUE} and the
upper-case spelling no longer matches. The raw \code{query} field is only read,
never mutated.
}
\examples{
urls <- c(
  "http://example.com/?utm_source=nl&id=42",
  "http://example.com/watch?v=abc&utm_source=x",
  "http://example.com/?id=99"
)
query_param_summary(urls)
query_param_summary(urls, level = "value")
# Preview a custom policy:
query_param_summary(urls, params_drop = "id")
}

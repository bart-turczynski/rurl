# Clear all rurl caches

Clears the memoization caches used by rurl functions. This is useful if
you need to free memory.

## Usage

``` r
rurl_clear_caches()
```

## Value

Invisibly returns NULL.

## Examples

``` r
rurl_clear_caches()
```
